# Public application name shown in the UI.
VITE_APP_NAME=EduTrackIlmziyo
# Supabase project URL, exposed to the client because it is public.
VITE_SUPABASE_URL=
# Supabase anon key, safe for browser use when RLS is configured correctly.
VITE_SUPABASE_ANON_KEY=
